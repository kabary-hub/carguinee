/**
 * Sentry backend — stub (désactivé en dev).
 * En production, configurer SENTRY_DSN dans .env pour activer le monitoring.
 */
export function initSentry() {
  // Sentry backend sera activé en production avec SENTRY_DSN
}
